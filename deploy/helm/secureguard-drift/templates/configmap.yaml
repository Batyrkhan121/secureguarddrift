apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "secureguard-drift.fullname" . }}-config
  labels:
    {{- include "secureguard-drift.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
